# Audit Progress — one contract at a time

Process per contract: (1) **extract the as-deployed source** (`solcInputs` that compiled to live bytecode) to `audit/asdeployed/<Name>/` and confirm it == live (Step B bytecode-equal, immutables masked); audit THAT, not the workspace `.sol` (which may have drifted ahead — see VAO). (2) deep multi-agent audit with adversarial verification + **non-admin** fund-flow + governance-handoff lens, (3) both clean → user agrees → check off.

**Diagram convention:** atomic-flow diagrams show ONLY non-admin paths (admin is being removed/handed off, so the closed-circuit proof must hold WITHOUT it). Admin/governance powers go in a separate box + the permanence watchlist.

Legend: ✅ done · ⏳ in progress · ⬜ not started · 🔴 needs live source from Etherscan/Sourcify (repo source older than live impl)

Order = VY flow: token → mint treasury (VYT) → its officers (VAO done) → reserve treasury (VRT, next — it's VAO's asset sink) → other officers.

**VAO live deps (for the system diagram):** VYT `0xe58E29c9…2974` (VY source) · VCO `0x2f024159…0051C` (cap accounting) · VRT `0x06087789…C832` (asset sink) · VY `0x597b2952…0454` · feeRecipient=VBBO `0x4b97d45d…2be2f6` · admin `0x8310ea7e…4a09` (=VYT admin) · WALLET_ROLE bot `0x3c3816e9…1672`.

| # | Contract | Source == Live? | Audit | Checked off |
|--:|----------|:-----:|:-----:|:-----------:|
| 1 | ValinityToken (VY) | ✅ byte-EXACT (non-proxy) | ✅ no Crit/High/Med; 3 Low, 4 Info, +VY-08 | ✅ user OK — fee→VBBO permanence noted |
| 2 | ValinityYieldTreasury (VYT) | ✅ workspace==as-deployed==live (UUPS impl `0x35a86beb…`) | ✅ done — no exploitable bug; 2 High + 2 Med = handoff/centralization | ✅ user OK — handoff conditions logged |
| 3 | ValinityAcquisitionOfficer (VAO) | ✅ **as-deployed==live = V2** (impl `0xc364f74e…`; solcInputHash `cb6d5b6c`; 64/64 ABI selectors in bytecode; workspace 857ln sha256 == as-deployed). **Live = permissionless V2** (`executeAcquireByLTV/MTP`, DAX-routed). | ✅ V2: **CLOSED (non-admin) by 2 balance invariants** (zero VY+asset retention); hardcoded VY→DAX→VRT; rescue blocks VY. 2 Low, 3 Info; dep CC-1 (DAX swap must be permissioned — verify at DAX audit). | ✅ **user OK 2026-06-01** |
| 5 | ValinityReserveTreasury (VRT) | ✅ **source==live (FULL 816-line, proven by metadata-hash `a1875f40…`)** — impl `0x5a2ce62e…`; 43/43 ABI selectors in bytecode (after fixing leading-zero-selector guard bug). NO reconstruction — earlier excision of `decreasePositionLiquidity` was MY tooling false-negative, retracted (user caught via metadata hash). | ✅ **CLOSED relative to roles, NO permissionless drain** (reserve vault; exits gated OFFICER/BUYBACK/VRYO/VLM/ADMIN). 0 Crit/High/Med/Low, 8 Info. Workflow `w41q4tth3` (18 conf/28 ref) + `decreasePositionLiquidity` re-audited (VRYO-only, collect→msg.sender fixed). | ✅ **user OK 2026-06-01** |
| 6 | ValinityLoanOfficer (VLO) | ✅ **source==live RECOMPILE-PROVEN** — workspace `ValinityLoanOfficer.sol` (1135ln) recompiled w/ Hardhat solc 0.8.27 → metadata hash IDENTICAL to live impl `0xd72e3fb7…` (`79ade5ae…`); only UUPS immutables differ. On-disk artifact is OLDER build (user redeployed latest from workspace via another chat). | ✅ **CLOSED-non-admin** — no arbitrary-dest leak; lending math verified sound (no tiny-payment extraction, conservation exact). 0 Crit/High, **1 Med (VLO-M1: migrateLoans cap-inflation — accepted: one-time bootstrap from prior ecosystem, locked after gov)**, 2 Low, 13 Info. Workflow `wy5ct28gd` reconciled (20 conf/12 ref). | ✅ **user OK 2026-06-01** |
| 7 | ValinityLiquidityManager (VLM) | ✅ **source==live metadata-hash PROVEN** — live impl `0xcB147742…` = commit **`c4bfc30`** VLM.sol (meta `d69e1a26…`, 24,433 B exact); DELEGATECALLed library **V3SnapbackGate** `0x5c3baf31…` = solcInput `211db5a0…` (meta `08a6c1e9…`, 5,129 B). On-disk artifact is stale V3; workspace `a89611b` is **1 ADMIN-only guard line ahead** (`setSnapbackPairs` dedup), not yet redeployed. As-deployed saved to `asdeployed/ValinityLiquidityManager/`. | ✅ **CLOSED (non-admin), no arbitrary-dest leak** (mint→VRT, swap→self, collect→self, sweep→VRT all hardcoded; delegatecalled lib is view-only). 0 Crit/High/Med, **1 Low (VLM-L1: bounded snapback zap MEV** — slot0-anchored minOut; capped ±3% TWAP guard + 6h cooldown + **TWAP-priced** gate (not cheaply bypassable); ≤4 cyc/day, <1% pos/cyc, no redirect**)**, rest Info. Workflow `wlekavf32` reconciled (24 agents, 10 surv/3 ref). Corrected: twapInterval/maxTickDev NOT frozen (UUPS-retunable). | ✅ **user OK 2026-06-01** |
| 8 | ValinityReserveYieldOfficer (VRYO) | ✅ **source==live metadata-hash PROVEN** — live impl `0x89f256f0…` = commit **`c4bfc30`** VRYO.sol (meta `0c18431c…`, 18,110 B). artifact.deployedBytecode+metadata==live (bytediff MATCH, only 2 UUPS immutables) BUT recorded `solcInputHash a50f3f1a` is **STALE** (recompiles to `d3752a6a`, VAO-style pointer bug — don't trust). Workspace `a89611b` is 1 commit AHEAD (`fff08e0f`), not deployed. V3ZapMath inlined (no delegatecall). As-deployed saved. | ✅ **CLOSED — NO arbitrary-dest leak ANYWHERE** (not even admin: `rescueTokens()`/`sweepUsdcDust()` have no `to`, settle to VRT — stronger than VLM). Only permissionless entry `execute()`; all sinks hardcoded. 0 Crit/High/Med, **1 Low (VRYO-L1 bounded execute zap MEV** — slot0-anchored minOut; capped VLM ±3% TWAP + 0.5% slippage + 2.5% drift gate; no redirect, opportunistic, tiny notional**)**, rest Info. **Cap-accounting PASSED — no VLO-M1 inflation** (deploy/recall symmetric, per-asset caps self-correcting + floor-bounded). Workflow `wy9fa82g7` reconciled (22 agents, 5 surv/6 ref). **6 VCO obligations** logged. | ✅ **user OK 2026-06-02** |
| 8 | ValinityFloorOfficer (VFO) | ✅ **source==live PROVEN** — addr `0x3d9d78CD…59D51` (**NON-PROXY, NON-UPGRADEABLE**, constructor-based, impl slot empty). As-deployed source keccak `0xcf265d27…` == `deployment.metadata.sources[VFO].keccak` = **git `8a8b795`** ("VFO optimazations") = `.v1.bak` (byte-identical). 30/31 closure files byte-identical to `8a8b795` (31st=VYT, uncommitted variant, immaterial — VFO never calls VYT methods). `deployedBytecode` meta IPFS `1220d0b9bbdd…` == live; 6 immutables confirmed == live system addrs. **⚠ Workspace `.sol` (627ln, `5d96a85`) = UNDEPLOYED UUPS rewrite (permissionless `executeFloor`, hardcoded routing) — DO NOT audit it; 2 undeployed newer commits exist (`dec5b8d`,`5d96a85`).** As-deployed saved. | ✅ **CLOSED relative to roles — no arbitrary-dest exfil even by a fully-compromised operator** (every settlement exit hardcoded {VYT, VRT→self, Balancer repay, profitRecipient}; operator's only freedom = swap routing, contained by per-step **output-increase invariant** L498 sampled *after* `router.call` → defeats recipient-redirect/`sweepToken`/`multicall`, + clean-state checks). **NO surviving Crit/High *technical*** (contested [High] sweepToken bypass REFUTED — output check is post-call). **OPERATOR-GATED** (`initiateFloor` onlyOperator `0x6B700Bd4` EOA; not permissionless). **Golden-rule TEXTBOOK** (`totalCapReduction==vyAmount` → cap↓==VY-to-VYT; `withdraw=capReduction·reserve/cap` preserves backing ratio → no under-backing; dust over-backs). Roles: VRT.BUYBACK + VCO.OFFICER (least-priv). 41 ops, standing bal=0. **Residual: VFO-H1** setProfitRecipient admin profit-diversion (profit-only, not principal); **M1** operator self-sandwich MEV (no minOut; principal-safe); **M2** rescueToken arbitrary token+dest (no VY block; bal~0); **M3** unverified withdrawForBuyback return (needs VRT-bug+operator collusion); Lows (dup-asset hygiene, FoT-revert, router-whitelist, hook-reentrancy). Non-upgradeable=frozen. Workflow `wkqttweuq` reconciled (64 agents, 19 surv/37 ref). **Reconciles VCO `0x3d9d78CD` = VFO (was "buyback-2").** | 🔁 **CIRCLE BACK — NOT closed.** User will DEPLOY THE LATEST VFO (the workspace UUPS rewrite `5d96a85`: permissionless `executeFloor`, hardcoded routing) → this `8a8b795` audit will be OVERWRITTEN; re-audit the new version once deployed. |
| 9 | ValinityPortal | ✅ byte-EXACT (non-proxy) | ⬜ | ⬜ |
| 10 | VDAX | as-deployed==live; check workspace drift | ⬜ | ⬜ |
| 11 | ValinityCapOfficer (VCO) | ✅ **source==live METADATA-HASH PROVEN (gold standard)** — live impl `0x294841f3…` meta `1220ba8bec80…` == recompile of **git commit `e268bbd`** via build-info `983cffc3` (also `21058a62`) (solc 0.8.27/runs=100/cancun). **Workspace HEAD `8bb172a` is ONE COMMIT AHEAD (undeployed; `1220cdbfc76f…`, 11992 B) — verification essential.** Closure = 5 repo files (+VRT/VY/V3LiquidityMath transitive) + 30 OZ v5. As-deployed saved. | ✅ **CLOSED ledger — 0 permissionless-exploitable** (MAX-RIGOR, 9 dims). Pure accounting (holds NO tokens, transfers nothing); every cap/registry/utilization mutator OFFICER/ADMIN-gated; arithmetic sound (overflow-checked increases, floor-guarded decreases, saturating reads). **VY CANNOT be under-backed via any permissionless/view/oracle path** (over-statement only via ADMIN setAssetCap/_authorizeUpgrade or malicious officer). Σcaps≈57,676 ≪ circulating 371,202, utilized=0 → no standing overstatement. **2 VCO Medium: (1) V3-LP undercount** (TVL/LTV-F read only raw `balanceOf(VRT)`, ignore VRT's V3 LP NFTs ~100×; misrepresents backing in views + mis-routes LTV-F cap-selection; conservative & reshuffle-only → no under-backing; NOT fixed in newer ver) **(2) VGO `0x0a6C2117` unaudited holds OFFICER_ROLE** (audit/revoke). + VLO-M1 (accepted). Lows: setAssetCap arbitrary, removeAsset. **Golden-rule conservation enforced per-OFFICER (VCO is faithful but BLIND — amounts officer-supplied).** Dominant levers `_authorizeUpgrade` > `setAssetCap` → timelock both. Workflow `wdkc3ozml` reconciled (97 agents, 10 surv/77 ref). **Conservation obligations: VBBO/buyback-2/VLO/VAO/VRYO(closed-leg)/VGO; VAO-TWAP; V3-LP fix.** | ✅ **user OK 2026-06-03** |
| 10 | ValinityDAX (DAX) | ✅ **source==live TRIPLE-CONFIRMED** — proxy `0xD256C672`→impl `0x8efde23e`; bytediff artifact==live (2 immutables) + metadata ipfs `7598b902` identical across artifact/recompile-from-solcInputs(`cb6d5b6c`)/live. As-deployed saved (ValinityDAX 877ln + VDAX 166ln). | ✅ **CLOSED relative to roles/whitelists — ZERO permissionless entry, no DAX-internal break.** AMM/LP math **sound & value-conserving**. **0 permissionless-exploitable, 1 Medium (DAX-M1/CC-1 — cross-contract, lives in VEO+SR), 3 Low** (L1 single-sided LP round-trip leak [SR-mitigated]; L2 basket non-std-asset desync incl. live FoT PAXG; L3 addPool no-validation), rest Info. **CC-1 RESOLVES via pivot**: officers' `minOut=0` no-sandwich rests on **VEO + StakingRouter**, not DAX. Workflow `w91m0w9q5` reconciled (31 agents, 16 surv/5 ref). **6 VEO/SR/VARO obligations** logged. | ✅ **user OK 2026-06-02** |
| 13 | ValinityExchangeOfficer (VEO) | ✅ **source==live METADATA-HASH PROVEN (gold standard)** — live impl `0xd20b0c8b…` metadata ipfs `1220f6c52b99…` == recompile of workspace `contracts/exchange/ValinityExchangeOfficer.sol` (HEAD `11dcc32`, git clean) via build-info `21058a62` (solc 0.8.27/runs=100/cancun). **The on-disk deployment json is a STALE/different-settings compile (meta `122060cbd903…`, bytecode ≠ live) — DO NOT trust** (live UUPS-upgraded past the committed deploy record). Closure = 1 repo file + 22 stock OZ v5. As-deployed saved. | ✅ **CLOSED non-admin — 0 Crit/High/Med/Low** (all Info). Every exit ∈ {msg.sender, VBO, vdaoCreator, transient self}; no caller-supplied recipient; holds ~zero between txs. **CC-1 FULLY DISCHARGED — no atomic sandwich** (one swap/tx, nonReentrant, no multicall; **officers don't route through VEO** ⇒ residual is officer-side multi-tx MEV). Live **DORMANT** (`varo=0`, `TraderRegistered=0`). **Handoff load-bearing: migrate `ADMIN_ROLE` (not just DEFAULT_ADMIN) — `_authorizeUpgrade`@1002 is ADMIN_ROLE-gated.** Workflow `wqoqdl1g7` reconciled (46 agents, 5 surv all-Info/32 ref). **VARO + StakingRouter + officer-MEV obligations logged.** | ✅ **user OK 2026-06-02** |
| 14 | ValinityStakingRouter (VSR) | ✅ **source==live METADATA-HASH PROVEN (gold standard)** — live impl `0xe00db9e6…` metadata ipfs `1220cb0b8a8e…` == recompile of workspace `contracts/staking/ValinityStakingRouter.sol` (HEAD `8bb172a`, git clean) via build-info `7789ac48` (solc 0.8.27/**runs=1**/cancun). **On-disk deployment json STALE/different-settings (meta `12203a7a9ee0…`, 14745 vs live 24076 B) — DO NOT trust** (live UUPS-upgraded past committed record). Closure = 1 repo file + 25 OZ v5. As-deployed saved. | ✅ **CLOSED non-admin — 0 permissionless-exploitable.** LIVE & holds real funds (~564,647 VY staked, 765,748 VDAX custody, 0 raw VY). Every exit ∈ {msg.sender, buybackOfficer}; LP custody never leaves except burned back in. **CC-1/DAX-L1 DISCHARGED** by ≥30-day lockup (deposit↔withdraw non-atomic, opposite-dir) + principal-clamp (surplus→BBO, staker no profit leg) + admin-frozen index. "Exactly principalVY" value-conserving; YO reserve non-drainable (minVyOut msg.sender-scoped → self-sandwich neg-EV). Entire residual = **admin-trust: 1 Crit (UUPS `_authorizeUpgrade`=DEFAULT_ADMIN, dominant lever) + 1 High (adminExtract→arbitrary `to`) + 2 Med (officer-setters; YO-solvency freeze) + 2 Low** — ALL handoff. Plain OZ AccessControl (no upgrade delay) → gov must add external timelock + adminExtract cap; migrate BOTH roles + renounce; never lower 30d tier floor. Workflow `w5etym0g7` reconciled (69 agents, 12 surv/48 ref). **VYO-solvency (HARD) + VRYO-role obligations logged.** | ✅ **user OK 2026-06-02** |
| 15 | ValinityGasOfficer (VGO) — **BRAND-NEW redesign** | ✅ **source==live METADATA-PROVEN** — proxy `0x0a6C2117` → impl `0x7c35d9d7` (meta `1220552ad640…`) == build-info `d7b281e8` compile of the **dirty workspace** `ValinityGasOfficer.sol` (391ln, keccak `0xa08a0dcf`; deployed from uncommitted workspace). solc 0.8.27/runs=100/cancun. v1 was a diff addr `0x6d6AE9de`. As-deployed saved. | ⏳ **No custody drain — keeper-reward engine; holds no user funds, pulls no VY.** Mints **VGC** (separate gas-credit token; VGO=sole minter) to officer-supplied keepers at **1.25× metered base-fee gas cost** (priced via private-DAX spot quote), globally capped by **VGC's per-epoch ceiling** (in the VGC token). Prior VY-pull/VCO-cap surface GONE (inert legacy slots). **DORMANT live** (`vgc=0` → payReward reverts). **⚠ VGO-H1: VCO-CC4 NOT resolved — VGO STILL holds `VCO.OFFICER_ROLE`=true on-chain** (comment says "revoked" but it wasn't) → admin upgrade could weaponize it for phantom cap inflation → **revoke the role**. **M1** 1.25× gas-burn farming by a compromised officer (~25%, bounded by VGC ceiling); **M2** VGC token unaudited (ceiling lives there). Lows: spot-quote rests on DAX permissioning, maxBaseFeeWei=0, rescue dust. OFFICER_ROLE: VLM/VBBO/`0x7a0E5824`. **H2** `_authorizeUpgrade` dominant (amplified by H1+sole-minter); **M3** DAX whitelisted-swapper quote-skew (VBBO on both). Auth/reentrancy/storage sound (per-officer transient slot, V3 layout preserved). Workflow `wdvoxe1c0` reconciled (71 agents, 51 surv [many positive]/10 ref). **Blocking pre-handoff: `vco.revokeRole(OFFICER_ROLE, VGO)` + audit VGC before `wireVgc`.** | ⬜ **awaiting user OK** |
| 17 | ValinityGovernanceCommittee (VGC) — gas-credit + gov token | ✅ **source==live METADATA-PROVEN** — addr `0xe595309a` (**NON-PROXY / immutable**); build-info `32bd69ca` meta `12200f327310…` == dirty workspace `ValinityGovernanceCommittee.sol` (257ln, keccak `0x80d4213d`). solc 0.8.27/runs=100/cancun. As-deployed saved. | ⏳ **Emission provably bounded — sound cap/kill-switch (validates VGO-M2).** Plain ERC20+Permit, NO fee/NO burn/NO hook/NO admin-mint/NO balance-move/NO upgrade. Per-epoch ceiling ≤`epochMintBps`(≤0.25%/wk) of UNMINTED supply (frozen at epoch start); totalSupply asymptotic < MAX_SUPPLY 7M forever; `epochMintBps=0`=halt; `epochMintBps`≤immutable `MAX_EPOCH_MINT_BPS=25` (admin can only slow/halt, never accelerate). Sole minter ONE-TIME-locked. **Live: totalSupply=1M (seed to KMS), minter=0x0 UNSET (doubly-dormant w/ VGO.vgc=0), epochMintBps=25, admin=0x8310eA7E.** Proven by induction (totalSupply<MAX_SUPPLY forever, no underflow), no epoch-compounding, uint96-safe, `getMintAllowance` mirrors `mint`, stock OZ (no fee/burn/hook). **NO Crit/High/Med.** Residual (Low/ops): L1 one-time setMinter activation-risk (point at audited VGO), L2 admin halt (kill-switch, ~7d latency), L3 transferAdmin no zero-check, L4 1M seed at KMS (gov centralization). Workflow `w5mu2e3r7` reconciled (73 agents, 4 surv/61 ref). | ✅ **user OK 2026-06-05** |
| 16 | ValinityYieldOfficer (VYO) | ✅ **source==live METADATA-HASH PROVEN (gold standard)** — live impl `0x08a4de6b…` meta `1220e8c4be47…` == recompile of **uncommitted-workspace** `contracts/officer/ValinityYieldOfficer.sol` via build-info `21058a62` (solc 0.8.27/runs=100/cancun). **HEAD `0fb4122` does NOT match — user redeployed from workspace without committing (VLO pattern).** On-disk deployment json = different compile. Closure 1 repo + 25 OZ v5. As-deployed saved. | ✅ **CLOSED non-admin by physics — 0 permissionless-exploitable.** VYO is a VY MINTER (VYT.OFFICER_ROLE=true). VY minted at only 4 `vyt.pullTokens` sites; permissionless sinks (claimYield/claimAssetYield) msg.sender-scoped + recipient-hardcoded + clamped to `maxGross`; topUps onlyRouter(VSR). Mint **bounded** (9% cap, frozen snapshot, self-throttling rate→0 as VYT free-VY drains), **backed** (every pull booked 1:1 into VCO = **cap golden-rule increment**, confirmed correct-by-design), **value-conserving** (reservation conservation-neutral; clamp-to-0 only on decrements → biases rate DOWN, never inflates). VSR backstop bounded+backed, only DoS-able (~12× cover), not drainable. Entire residual = **admin-trust; DOMINANT lever `_authorizeUpgrade`** (upgrade inherits OFFICER_ROLE → unbounded mint) > `setRouter`; both roles → timelock + renounce. Workflow `wf_b7bb0f6d-03a` (resumed `w0ximbq0f` after overnight stall; 63 agents, 5 surv all-Info/Low / 49 ref). **VYT + VCO(golden-rule sink) + VARO + VSR obligations logged.** | ✅ **user OK 2026-06-03** |
| 9 | ValinityBuybackOfficer (VBBO) | ✅ **source==live BYTE-IDENTICAL proven** — proxy-pointer artifact (impl `0xf311e729…`, no metadata/solcInputHash). bytediff: artifact.deployedBytecode==live (2 UUPS immutables only); masked recompile of workspace `b77d8d7` = **0 residual diff bytes**. artifact metadata==live (`0c597412…`); workspace recompile metadata `d3396fd2…` differs by comments only (zero code diff). As-deployed saved. | ✅ **CLOSED (permissionless) — no arbitrary-dest** (sinks hardcoded; `rescueToken` ADMIN-only + blocks VY). Economics **value-conservative & non-drainable**. 0 Crit/High, **1 Medium (VBBO-M1/CC-1: `minOut=0` DAX swaps** — no-MEV guarantee delegated to DAX permissioning; not a redirect; rate-limited 2h+size-bounded; **HARD PREREQ = audit DAX before handoff**, same CC-1 as VAO), **1 Low** (VGC donation swap, dormant), rest Info. VGC donation disabled live. Workflow `wrc1qyt6q` reconciled (20 agents, 9 surv/1 ref). **5 DAX + 2 VCO obligations** logged. | ✅ **user OK 2026-06-02** |
| 18 | ValinityAllianceRegistrationOfficer (VARO) — **BRAND-NEW** | ✅ **source==live METADATA-PROVEN (impl + lib)** — proxy `0x514F0ABf` → impl `0xeC4B6401` (UUPS, meta `1220e9088c91…`) + DELEGATECALL lib `0x6A0E4A9D` (`VAROReferralSettleLib`, meta `1220926193…`). deployment-json deployedBytecode meta == LIVE for both; `metadata.sources` keccak256 == workspace `.sol` (byte-identical, **git-clean HEAD `a85f449`**). solc 0.8.27/runs=1/cancun. impl 24,405 B + lib 4,163 B. As-deployed saved (both files). | ⏳ **CLOSED relative to roles — no arbitrary-dest exfil in any non-admin path.** Paid-tier gateway + referral-revenue accountant + V-DAO launcher (1823+287 ln). Every exit ∈ {VBBO hardcoded init-only, msg.sender, `predictBuilder(msg.sender)`, bound V-DAO+its pools, AMM pools}; **no `setVbbo`/`setVy`/`setVyt`/`setVco`, no `rescueToken`**. **VY mint golden-rule-correct**: only `_payOut` does `vyt.pullTokens(paid)`+`vco.addToHighestLTVFCap(paid)` — pullTokens **reverts-on-shortfall, never partial** (verified vs as-deployed VYT) → cap increment exact, cushion-throttled, custody-0. **Referral credit checkpoint-idempotent** (settleMine/sweep), bounded by real fees×bps (≤99%); self-dealing net-negative. Residual: **C1** `_authorizeUpgrade` inherits all officer roles (dominant); **H1** un-checkpointed PUSH (`vpo`, `REVENUE_PUSHER_ROLE`) over-credit→over-mint (trust); M1 `setTierUsdcPrice` no bounds; M2 `setHlFactory` redirects user CCTP $100; M4 95% bps mint magnitude (by-design); M5 one-shot vgcDeployer/Recipient irreversible; Lows (cached vdaoDaxSecondPoolId design-risk, minOut=0 sub-$10 buyback=protocol-side not user, spot-V2 quote w/ caller bounds, usdcToAsset tiny-amount truncation, trapped dust, dual-role migrate-both, setHouse post-bootstrap lock, ETH-refund 23k cap, sweep batch-check-after-beginReward=self-inflicted). **LIVE: FULLY DORMANT/UN-WIRED** — holds NONE of its roles (VYT.OFFICER=false→can't mint; VCO/VDAX/VDAO-DAX/VSR all false), factory/cctp/hlFactory/weth/wbtc/paxg/vpo=0, vdaoBootstrapped=false, zero activity. **RECONCILED** workflow `wau2dte5v` (30 agents, 31 raw→17 surv/4 ref/10 Low-Info · 1C/2H/5M/9L, ALL admin-trust/handoff/ops, **0 permissionless-exploitable**). | ✅ **user OK 2026-06-06** |
| 19 | ValinityVDAODAX (VDAO-DAX) — **BRAND-NEW** (V-DAO second-leg AMM) | ✅ **source==live METADATA-PROVEN** — UUPS proxy `0x37Cd61b3` → impl `0xe2883b42` (meta `1220301b9911…`). deployment-json deployedBytecode meta == LIVE; `metadata.sources` keccak256 == workspace `.sol`+interface (byte-identical, **git-clean HEAD `a85f449`**). solc 0.8.27/runs=100/cancun. impl 7,652 B. As-deployed saved (both files). | ⏳ **CLOSED / liquidity permanently LOCKED relative to roles.** Private VY-agnostic constant-product AMM (NO AMM fee) holding every V-DAO's 2nd leg (asset/V-DAO + V-DAO/V-DAO); sibling of main DAX. **NO LP token, NO deposit/withdraw/remove/skim path** — reserves only grow (addPool seed / donate / swap-in) or shrink via a **value-conserving k-preserving swap** (`swapExactIn`, onlySwapWhitelisted); `rescueTokens` **reverts on any isPoolToken** (admin can't pull pool reserves). FoT-safe balance-delta accounting; listing rule (assetAllowed OR mainDax.hasPool); pools **isolated** (no shared basket). `donate` permissionless but add-only (donor can't swap→no profit). Residual: **C1** `_authorizeUpgrade` IS the liquidity boundary (a malicious upgrade adds a drain → pulls all reserves; no timelock); M1 swap-whitelist curation; M2 assetAllowed poison-asset (pool-isolated); Lows (donate skew, stuck direct-transfers, creation-time-only listing, dual-role). **LIVE DORMANT** — getNumPools=0 (no liquidity), no swappers whitelisted (swaps impossible), VARO=POOL_CREATOR✅, assetAllowed={USDC,WBTC,WETH,PAXG}, mainDax=`0xD256C672`(real DAX✅), admin=KMS. Workflow `wvrnl84j3` (running). | ⬜ **awaiting workflow + user OK** |
| 4b | ValinityMEVBotV2 (VMB) — **RE-AUDIT (new version)** | ✅ **source==live METADATA-PROVEN** — proxy `0x6f2F4580` **UPGRADED** to impl `0x98d7ee49…` (meta `12206c2d712d…`) == build-info `4515c727` compile of the **dirty workspace** `ValinityMEVBotV2.sol` (446ln, keccak `0x9daff5c3`; deployed from uncommitted workspace). solc 0.8.27/runs=100/cancun. **Prior audit was impl `0x16b66a22` (now replaced).** As-deployed saved. | ✅ **CLOSED — no arbitrary-dest exfil, even by a malicious whitelisted router.** **COMPLETE REDESIGN**: removed arbitrary `router.call(callData)`+`routerSelectorAllowed` (old CC-1 Med — GONE); keeper now supplies only `borrowVY`+typed `Leg[]`, **contract builds each swap** (recipient=this, minOut=0, 100% balance). Closed VY→…→VY loop + `SwapOutputZero`+`TokenInNotDrained`+`finalVY≥borrowVY`(repay)+`VYBalanceNotZero`(ends 0) ⇒ any diversion reverts atomically (repay invariant is the closure, not the whitelist). Treasury made whole every tx; no unbacked mint w/o upgrade. Permissionless but bounded (7d cooldown + **70 VY min-profit floor** live). **PRINCIPAL closed even vs malicious INTERMEDIATE router; profit-above-floor NOT (H-1 final-leg router skims `actualProfit−minProfitVY`).** Residual (all admin/upgrade-gated): **C-1** `_authorizeUpgrade` dominant (inherits VYT.OFFICER_ROLE → **cushion-bounded ~6.65M VY/call**, NOT unbounded — bot is standard officer); **H-1** final-leg router profit-skim (100% if floor=0); **H-2** `setMinProfitVY=0`; **M-1** `setVgo` unaudited-VGO trust (`vgo=0` live); **M-2** dual-role handoff trap; Lows. Whitelist=UniV2 Router02+UniV3 SwapRouter02. 14 arbs, bal=0. Workflow `wc33q498d` reconciled (26 surv/29 ref). | ⬜ **awaiting user OK** |

**Match methodology — ROBUST RULE (see `findings/system-source-truth-gap.md` + `findings/VAO-source-error-correction.md`):** Two independent legs are BOTH required:
1. **Bytecode leg:** artifact `deployedBytecode` == live runtime after masking metadata + immutables (`bytediff.mjs`).
2. **Source leg (NEW — this is the leg that failed on VAO):** the extracted `solcInputs` source must actually be the source that produced that bytecode. `artifact.solcInputHash` can be STALE/inconsistent (VAO's pointed at a non-deployed V1 while its bytecode was V2). `extract_asdeployed.mjs` now enforces this: ≥95% of the artifact-ABI selectors must appear in `deployedBytecode`, else FATAL. Confirm `_MANIFEST.json.guard.bytecodeSelectorRate ≈ 1.0` before trusting any extracted source.

"artifact-bytecode == live" alone is NOT sufficient — it proves the artifact matches live, not that the extracted source matches the artifact. **Always run the hardened extractor and confirm the guard passes.** (The workspace `.sol` may or may not match — verify, don't assume a drift direction: VAO's workspace turned out to be correct; never assume.) The 🔴 rows (8 contracts incl. P0 CapOfficer/BuybackOfficer) have a live impl newer than OR absent from any local `solcInputs` → need Etherscan/Sourcify source.
