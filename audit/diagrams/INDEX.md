# Fund-Flow Diagrams — Index (for the governance-handoff dev)

Hand this folder to the developer performing the admin → governance transfer. Each contract gets a fund-flow diagram proving where value can physically go ("closed circuit"), plus any address/config that becomes **effectively permanent** once the admin is locked into governance (Fase 4 = irreversible).

## ⚠️ Settings that become PERMANENT at handoff (cross-contract watchlist)
These are admin-settable today but, after Fase 4, only changeable via a passed governance proposal — so confirm each is correct **before** locking:

| Setting | Contract | Live value | Note |
|---|---|---|---|
| `transferFeeRecipient` | VY | **VBBO** `0x4B97D45d…2F6` | 1% transfer fee feeds the Buyback Officer. Confirm before lock. |
| **ADMIN + DEFAULT_ADMIN** | **VYT** | `0x8310eA7E…4a09` | **DISPOSITIVE.** Must be TimelockController(≥48h)+multisig, never EOA/bare Governor — controls both `migrateTo` (drain-all) and `_authorizeUpgrade`. |
| `migrateTo` destination | VYT | unrestricted | Governance can vote 100% of VYT's VY anywhere. Bound (allowlist/two-step) before lock or risk-accept. |
| `_authorizeUpgrade` (UUPS) | VYT | ADMIN-only | **Highest-value power in the token system** — upgrading VYT inherits the permanent sole-VY-minter. Add codehash/allowlist/timelock + dedicated UPGRADER_ROLE before lock. |
| **WALLET_ROLE holder** | **VAO** | `0x3c3816e9…1672` (bot) | **Non-admin trust anchor.** Drives `acquire()`; via the full-approval router `.call` + `>0`-only output check, a compromised key can route pulled VY to an external pool (≤25%/cooldown). Survives handoff — keep minimal/hardened (multisig). |
| `whitelistedRouters` | VAO | admin-settable set | Each whitelisted router gets full-balance approval + arbitrary `.call`. A malicious/over-permissive router = direct VY-diversion path. Curate to audited routers only; freeze before lock. |
| fee ceiling (`setPriceDisparityFeeBps`/`setLTVDisparityFeeBps`) | VAO | up to **100%** | Fee can route 100% of pulled VY to feeRecipient. Cap <100% in code/policy before lock. |
| `feeRecipient` | VAO | **VBBO** `0x4b97d45d…2be2f6` | Fee slice sink (same as VY transfer fee). Confirm before lock. |
| `poolCapBps` + TWAP pools | VAO | 2500 (25%); admin-set pools | Per-tx size limiter uses flash-inflatable spot V2 reserves; LTV-F TWAP pools must be high-cardinality. Verify before lock. |
| **`_authorizeUpgrade` (C-1)** | **MEVBotV2 (new impl `0x98d7ee49`)** | ADMIN_ROLE, **no on-contract timelock** | **#1 lever / dominant.** UUPS upgrade bypasses every closed-loop invariant; bot holds VYT.OFFICER_ROLE → malicious impl calls `pullTokens(attacker,…)` with no repay. **Cushion-bounded ~6.65M VY/call** (standard officer; VYT CUSHION 350k/TARGET 7M), repeatable. → governance TimelockController + real upgrade delay; migrate **both** DEFAULT_ADMIN+ADMIN, **readback gov holds BOTH before any renounce** (dual-role trap M-2). |
| `setVgo` (M-1) | MEVBotV2 | `vgo=0` (disabled live) | Enables the keeper-reward bracket via the **UNAUDITED VGO**; VGO would need VYT.OFFICER_ROLE → could `pullTokens` VYT during `payReward` (VMB itself can't be drained). **Do NOT call until VGO is audited.** Timelock. |
| `setMinProfitVY` (H-2) | MEVBotV2 | live **70 VY** | No lower bound — setting **0** removes both the anti-grief floor and the H-1 cap (a malicious final-leg router could then skim 100% of profit), and enables VGO-reward farming when vgo set. Keep > 0; add `vgo!=0 ⇒ floor!=0` guard at next upgrade. |
| `whitelistedRouters` (H-1) | MEVBotV2 | UniV2 Router02 + UniV3 SwapRouter02 | New model: contract builds each swap (no caller calldata) — **principal is safe even vs a malicious router**, but a malicious **final-leg** (VY-terminal) router can skim profit-above-floor. Governance-voted, canonical routers only; freeze before lock. |
| `setBuybackOfficer` (L-1) | MEVBotV2 | VBBO `0x4B97D45d…` | Redirects 100% of arb profit (not principal). Timelock; keep = VBBO. |
| MEVBot admin key | MEVBotV2 | `0x8310ea7e…` (= VYT/VAO/VFO key) | Same KMS key controls bot upgrade+config AND the treasuries. Concentrated until governance handoff; **no bot-level timelock** (relies on governance pipeline). No rescue function exists (verify no upgrade adds one). |
| `migrateTo` newTreasury | VRT | unrestricted | ADMIN drains all reserves+VY to an arbitrary address (blocked while V3 NFTs active). Bound/allowlist before lock. |
| reserve role-holders | VRT | OFFICER=VLO `0x8fd8d5eb…` · BUYBACK={VBBO `0x4b97d45d…`, **VFO `0x79A902864d…`** ✅ (migrated; old `0x3d9d78cd` revoked)} · VRYO `0xa95749f5…` · ~~VLM~~ eliminated | These officers can move reserves to arbitrary recipients (role-gated). **`0x79A902864d` = the migrated ValinityFloorOfficer (VFO), audited ✅** — `withdrawForBuyback` recipient hardcoded to `address(this)`, no arbitrary-dest in the non-admin path; the upgrade lever (VFO-C1) IS the reserve-drain handoff concern. Confirm each = intended contract before lock. |
| `migrateLoans` cap symmetry | **VLO** | one-sided (bug) — **accepted** | **Medium (VLO-M1) — DISPOSITIONED.** `_migrateLoan` omits `vco.decreaseAssetCap`, but a later repay calls `increaseAssetCap` → VCO cap inflated per migrated-and-repaid loan. User (2026-06-01): migrateLoans was a **one-time bootstrap from a prior ecosystem, locked after gov** → no future surface. **Residual pre-handoff check:** reconcile `Σ VCO asset caps` vs circulating-VY backing once, to confirm outstanding migrated loans didn't leave a standing cap overstatement. |
| `setInterestRatePerSecond` | VLO | ≤ ~101% APR (capped); retroactive | A maxed rate force-underwaters existing loans → all collateral routed to VYT. Confirm policy / timelock. |
| VLO recipients | VLO | fee+interest=VBBO `0x4b97d45d…`, underwater=VYT `0xe58e29c9…` | Confirm before lock (live values correct). |
| ~~VLM watchlist rows~~ — 🗑️ **VLM ELIMINATED** | ~~VLM~~ | holds 0 roles everywhere | **REMOVED 2026-06-07.** All prior VLM handoff items (twapInterval/snapback/slippage/rescueTokens/wiring) are MOOT — VLM is decommissioned and holds no roles on VRT/VCO/DAX. No handoff action. |
| **VRYO `_authorizeUpgrade` — 3-role blast radius** | **VRYO** | admin = KMS; holds DAX.RESERVE_OFFICER + VCO.OFFICER + VRT.VRYO_ROLE | **VRYO-C1 dominant.** A malicious VRYO upgrade can drain DAX pool **asset** reserves (`reserveExtractAsset`→attacker), pull VRT reserves (`deployForYield`→attacker), and mutate VCO caps. Migrate DEFAULT_ADMIN+ADMIN → TimelockController; renounce EOA; treat VRYO + DAX as one trust unit (shared KMS). |
| **VRYO `assetDeployRatioBps` — the backing-quality dial** | **VRYO** | 6000 (60%) all assets — cap 9500 | **VRYO-M1 (economic, governance-accept).** How much hard reserve is deployed as one-sided DAX depth — and thus exposed to being arb'd/swapped into VY-in-pool. Higher = deeper DEX, lower hard-asset backing quality. Decide the acceptable level; **monitor `deployedAsset[A]` vs the live `pool.reserveAsset`** (it overstates after swaps). `setKeeperThreshold` (2.5%) tunes recall eagerness. |
| **DAX `RESERVE_OFFICER_ROLE`** | **DAX** | VRYO-only ✅ (KMS does NOT hold it) | **DAX-H1.** Can drain pool **asset** reserves (never VY) to any address. Grant ONLY to the audited VRYO; treat as admin-equivalent; never split to a second holder. A DAX `revokeRole`/upgrade can also strand VRYO's deployed assets (DAX-H2 — shared-KMS cross-bricking; recover via `adminExtractLiquidity`). |
| **DAX swap-whitelist incl. MEV bot** | **DAX** | {VBBO,VAO,VEO,SR,VGO,**MEV bot**✅} | The MEV bot is now swap-whitelisted and **load-bearing** (it rebalances the single-sided reserve-injection drift). Freeze to audited contracts; keep the MEV bot in (removing it strands the rebalancer). `reserveInjectAsset` books nominal (DAX-M3 PAXG over-count) — fix to balance-delta on a future upgrade. |
| **VCO obligations (from VRYO V3)** | **VCO** ✅ (audited) | `getAssetCap`/`getLTV`/`effectiveFloor`/increase+decreaseAssetCap drive deploy/recall | VRYO's cap-conservation (`globalCap = vco.getAssetCap + capVRYO`) + LTV-neutral deploy rest on VCO: caps officer-gated, LTV bounded, `decreaseAssetCap` floor-guarded. All hold per the VCO audit. Monitors must use **Σ `capVRYO(asset)`** (the deprecated `capVRYO_total` reads 0) and NOT double-count `deployedAsset` (already in daxTVL) — see [[project_vryo_v3_dax_redesign]]. |
| **`setDax`** | **VBBO** | `0xd256c672…` (=live DAX) | **HIGHEST-value VBBO knob.** `executeBuyback` swaps asset→VY on DAX with **minOut=0**; a malicious/mis-set DAX drains every buyback. Needs strongest governance control (timelock/allowlist) before lock. Same swap-venue trust as VAO. |
| **CC-1 (from VBBO+VAO) — FULLY DISCHARGED ✅** | **DAX ✅ → VEO ✅ + StakingRouter ✅** | swap-WL={VBBO,VAO,VEO,SR,VGO}; liq-WL={SR} | DAX gates swaps to the whitelist; public-facing members were **VEO** + **StakingRouter** — BOTH now audited & discharged. **VEO (`wqoqdl1g7`): NO atomic sandwich** (one swap/tx, nonReentrant, no multicall; officers don't route through VEO). **VSR (`w5etym0g7`): NO atomic round-trip of reserveVY** — the **≥30-day lockup** separates deposit (reserveVY↑) from withdraw (reserveVY↓), nonReentrant blocks composing them, and the principal-clamp routes all redemption surplus to BBO (staker has no profit leg) ⇒ DAX-L1 leak not extractable either. **Residual = officer-side multi-tx MEV** (mitigate via private relay / non-zero minOut on VAO/VBBO direct DAX swaps). CC-1 closed across the ecosystem. |
| **VEO: migrate `ADMIN_ROLE` (not just DEFAULT_ADMIN)** | **VEO** | both = `0x8310eA7E…4a09` | **Load-bearing.** `_authorizeUpgrade` @1002 is gated on **`ADMIN_ROLE`**, NOT `DEFAULT_ADMIN_ROLE`. Transferring DEFAULT_ADMIN alone does NOT neutralize upgradeability — must explicitly **revoke ADMIN_ROLE** from the legacy admin and place under TimelockController(≥48h). Migrate both atomically. |
| **VEO: `setVaro` activation switch** | **VEO** | `varo == address(0)` (DORMANT) | VEO is inert today (no registrations possible; all swaps revert). Going live = gov calls `setVaro(VARO)`. Verify dax/uniRouter/vbo/ondoGM/v2Router are canonical first. Don't rush; dormant is the safest state. VEO venue/sink setters all ADMIN_ROLE → timelock. |
| **VSR: UUPS upgrade = DOMINANT lever** | **VSR** | `_authorizeUpgrade`=DEFAULT_ADMIN (no delay) | **Critical handoff item.** Empty-body, no-timelock upgrade drains the ENTIRE backing (765k VDAX + 564k VY + 37 locked) and can delete the lockup/invariant — strictly broader than adminExtract. Plain OZ AccessControl (no DefaultAdminRules/upgrade-delay). Gov MUST add an external timelock + upgrade delay; migrate **both** DEFAULT_ADMIN (upgrade) + ADMIN (extract/rescue/officers) then **renounce**. |
| **VSR: `adminExtract(bps,to)`** | **VSR** | up to 100% distributable LP → arbitrary `to` | **High.** Pulls up to 765k−37 VDAX to any address, no cap/cooldown; bricks withdrawals + drains YO reserve (locked 1% excluded). Add per-period cap/cooldown + timelock before lock. |
| **VSR: 30-day tier floor** | **VSR** | `tierDurationSec` ≥ 30d (un-lowerable) | **This is the CC-1 lockup guard — NEVER lower it.** `setTierDuration` reverts below 30d. Curate stakeable assets (USDC/WBTC/PAXG/WETH live); never add FoT/rebasing/callback tokens (PAXG handling verified safe). |
| **VSR: YO-solvency covenant** | **VSR → VYO** | yieldOfficer=`0xA245C9D2…` | VSR's "exactly principalVY" + asset-value guarantees depend on VYO covering `topUpPrincipal`/`topUpAssetWithdrawal` (un-try/catch'd). Insolvent YO freezes loss-case VY + ALL asset withdrawals (DoS, not theft; gov-recoverable via setYieldOfficer+UUPS). **VYO audited ✅ — solvency = VYT mint-headroom (~12× live cover); keep gov-replaceable.** |
| **VYO: `_authorizeUpgrade` = DOMINANT lever** | **VYO** | ADMIN_ROLE (no delay) | **Highest-value mint lever in the whole system.** VYO holds **VYT.OFFICER_ROLE** (mints VY); an empty-body upgrade can swap to logic that calls `vyt.pullTokens(attacker, unbounded)` = **unbounded unbacked VY mint in one tx**, diluting all holders. Strictly dominates setRouter. ADMIN_ROLE → TimelockController + upgrade delay; migrate **both** DEFAULT_ADMIN + ADMIN then **renounce**. |
| **VYO: `setRouter`** | **VYO** | ROUTER_ROLE = VSR | grants ROUTER_ROLE; a malicious router pulls VYT to a router-supplied recipient (weaker than upgrade). Keep ROUTER_ROLE = audited VSR; treat setRouter as timelocked gov action. |
| **VYO: VCO booking = golden-rule sink** | **VYO → VCO** | capOfficer=`0x2f024159…` | Every VY VYO mints out of VYT is booked 1:1 into VCO's highest-LTV-F cap (`addToHighestLTVFCap`, 4 sites, un-try/catch by-design). **This is the [cap golden rule](#) increment.** VCO must absorb the accumulated bookings + never revert under normal op = the backing half. Carry to the **VCO conservation audit**. VCO immutable in VYO (no setter) = strength. |
| **VCO: `_authorizeUpgrade` + `setAssetCap` = under-backing levers** | **VCO** | both ADMIN_ROLE | **The two paths that can manufacture under-backed VY.** `_authorizeUpgrade` (replace all accounting logic) dominates; `setAssetCap` sets ANY cap bypassing the floor + officers + golden rule (phantom collateral). **Timelock + gov-gate BOTH** with a public delay; migrate DEFAULT_ADMIN + ADMIN then renounce. Also timelock setVAO/removeAsset/floor params. |
| **VCO: V3-LP valuation undercount** | **VCO** | TVL/LTV-F read raw `balanceOf(VRT)` only | **Medium.** VRT's reserves are mostly Uniswap V3 LP NFTs (per [[project_vlm_nft_ownership]]) — invisible to `balanceOf` → TVL/LTV-F understate backing ~100× live (floorPrice≈0, misleading to governance) and the LTV-F heuristic mis-routes cap selection. Conservative & reshuffle-only (no under-backing). **NOT fixed in the newer undeployed version.** Fix or formally document before relying on VCO views for governance. |
| **VCO: VGO OFFICER_ROLE — RESOLVED ✅ (revoked)** | **VCO ← VGO** | VGO=`0x0a6C2117…E827` | **VGO-H1 CLOSED.** The prior blocking item is **fixed on-chain: `VCO.hasRole(OFFICER_ROLE, VGO) == false`** (verified). VGO no longer holds any VCO power — even a malicious VGO upgrade cannot touch caps. No standing action. (Historical: the v4 comment claimed "revoked" before it actually was; the v5 migration completed the revoke.) |
| **VGO: `_authorizeUpgrade` (sole VGC minter)** | **VGO** | admin `0x8310eA7E`; LIVE (vgc wired, 59 rewards) | **VGO-D dominant lever — but bounded.** `_authorizeUpgrade` (ADMIN) controls the sole locked VGC minter; **blast radius is capped by VGC's own per-epoch ceiling** (no VY/reserve/cap reach — VGO holds no VY custody, VCO.OFFICER revoked). Co-locate VGO.ADMIN_ROLE + **VGC.admin** (epoch-bps halt) under the SAME timelock so minter + kill-switch are co-controlled. Migrate DEFAULT_ADMIN+ADMIN atomically; renounce EOA. Reward knobs (band/tipCap/perCall/baseFee) can only LOWER payout. Re-run `wireVgc` after any DAX reindex (VGO-L3). |
| **VCO golden-rule = per-OFFICER conservation** | **VCO ← officers** | OFFICERs: VLO,VAO,VYO✅,VBBO,**VFO `0x79A902864d`✅**,VRYO; ~~VGO~~(revoked) | VCO is a faithful but **BLIND** ledger — cap amounts are officer-supplied; VCO can't tell an open flow from a closed leg. Conservation verified at each officer: **VFO ✅ (migrated to `0x79A902864d` — FloorOfficer; cap↓==VY-to-VYT, withdraw=vyAmount·reserve/cap preserves backing, textbook-correct & caller-immune even though now permissionless)**, VBBO (swap-then-cap atomicity), VLO (migrateLoans locked), VAO (TWAP robustness + officer), **VRYO (closed-leg must net-zero)**. **VGO no longer an OFFICER on VCO** (revoked ✅). VLM eliminated. ✅ |
| **swap-whitelist membership** | **DAX** | {VBBO,VAO,VEO,SR,VGO} (live) | **#1 DAX handoff knob** — `updateSwapWhitelist` is ADMIN; adding an unvetted swapper enables sandwiching officers' minOut=0 flows. Freeze to audited contracts before lock; never add an unvetted swapper. |
| `addPool` poison-asset | DAX / VARO (POOL_CREATOR_ROLE) | basket holds LINK + 2 launched tokens | VDAX is ONE basket across all pools; a malicious/odd added asset could brick whole-basket `withdraw` or skew accounting for all holders. VARO is the only gate — audit VARO + curate assets. |
| `setVgcToken`/`setVgcUniV3Pool`/`donationBps` | VBBO | all 0 (donation DISABLED) | If governance enables the VGC LP donation post-handoff, ≤5% of VY is JIT-capturable by design. Decide deliberately. |
| `rescueToken` | VBBO | arbitrary `to` (ADMIN, **blocks VY**) | Can pull non-VY strays (WBTC/WETH/PAXG/VGC) anywhere post-handoff. Governance timelock; VBBO holds mostly VY (protected). |
| **VFO `_authorizeUpgrade` — 3-role master drain lever** | **VFO (new proxy `0x79A902864d…`)** | admin = KMS `0x8310eA7E`; holds VRT.BUYBACK + VCO.OFFICER + VGO.OFFICER + VY-whitelist | **VFO-C1 dominant (NOW UPGRADEABLE).** The migrated VFO is a UUPS proxy; a malicious impl inherits all 3 roles → **sweep 100% of VRT reserves** (`withdrawForBuyback`→attacker, Critical) + **unbounded `increaseAssetCap`** (corrupts golden rule, High). Migrate DEFAULT_ADMIN+ADMIN → TimelockController atomically **with VRYO + DAX** (shared KMS = one trust unit); add codehash/upgrade-delay; renounce EOA. `DEFAULT_ADMIN` administers `ADMIN` → migrate BOTH or the old key re-grants the lever (M2). |
| **VFO: venue/recipient setters** | **VFO** | usdc/v2Router/v3Router/balancerVault/v3Factory/v3Quoter/buybackOfficer (all canonical) | **High (VFO-H1).** `setUsdc`/`setV2Router`/`setV3Router`/`setBalancerVault`/`setBuybackOfficer` can re-point the "hardcoded" sinks to attacker venues/addresses without a full upgrade (changes WHAT the hardcoded addresses are). Timelock all; freeze to canonical + publish. |
| **VFO: backing-ratio read excludes VRYO-deployed reserve** | **VFO ↔ VRT/VRYO** | `reserve = balanceOf(VRT)` only | **Medium (VFO-M1) — MONITOR, not a leak.** VFO sizes `withdrawAmount = vyAmount·reserve/cap` off the **on-hand** VRT balance, which now understates true backing (VRYO deploys part of the reserve into DAX). Effect = VFO **under-withdraws** (conservative over-back); cap conservation stays exact. Same family as VRYO-M1 — monitor on-hand VRT + VRYO `deployedAsset` vs cap. |
| **VFO: permissionless + bounded lows** | **VFO** | execPaused=false; LIVE | Permissionless `executeFloor` is **safe** (profit→VBBO not caller, bad sizing reverts, flash-spoof blocked). Lows: `vryo.execute()` un-try/catch'd → a VRYO revert rolls back an already-settled defense (liveness only — wrap in try/catch on next upgrade); PAXG-FoT bricks PAXG defense if its fee turns on (DoS); `rescueToken` blocks VY but **not** reserve assets (bal~0); `setVryo` can install a griefing vryo. None permissionless-exploitable. |
| **VARO: `_authorizeUpgrade` + role grants** | **VARO** | admin `0x8310eA7E`; holds NO roles yet (dormant) | **VARO-C1 dominant lever.** Once wired, VARO holds VYT.OFFICER (mint VY) + VCO.OFFICER (cap) + VDAX POOL_CREATOR+swap-WL + VDAO-DAX POOL_CREATOR + VSR.VARO_ROLE + VGO.OFFICER. A malicious UUPS upgrade (ADMIN_ROLE, no on-contract timelock) weaponizes ALL of them (unbounded mint/cap/pool). Grant each role only when ready; migrate DEFAULT_ADMIN+ADMIN atomically → TimelockController; renounce EOA. |
| **VARO: `vpo` + `REVENUE_PUSHER_ROLE`** | **VARO** | `vpo=0`; no pushers (dormant) | **VARO-H1 — minter-adjacent trust.** `notifyReferrerPerpCredit` (vpo) + `notifyInviteeRevenue` (REVENUE_PUSHER_ROLE) credit `pendingVY` from a caller-asserted amount with **no checkpoint** → a compromised/buggy pusher inflates claimable freshly-minted VY (cushion-bounded). Grant ONLY to audited contracts; consider a per-epoch push cap on upgrade. |
| **VARO: `setTierUsdcPrice` / `setBps`** | **VARO** | tiers $0.50/$10/$110/$2000; bps 50%/95% | **VARO-H2 / M4.** `setTierUsdcPrice` has NO floor/ceiling (set 0=free tiers + free V-DAO launches + DoS, or huge). `setBps{Standard,VDAO}` ≤99%/9.9% → near-total referral payout = large ongoing VY-mint magnitude. Validate non-zero + sane values + timelock before lock. |
| **VARO: `setHlFactory` / `setCctp` / `cctpActivationUSDC`** | **VARO** | hlFactory/cctp `0` (unset); cctpActivation $100 | **VARO-H1 / M2 / M3.** CCTP `mintRecipient = hlFactory.predictBuilder(user)` — an admin-rotatable malicious `hlFactory` redirects EVERY user's bridged $100 to an attacker (4 entry points, no L1 recovery; user funds). `cctpActivationUSDC` has no upper bound (MEV-inflated payment). `hasBuilder` is set before CCTP delivery confirmation → a misconfigured `cctpHLDomain`/failed bridge strands $100 with no retry. Confirm `hlFactory` canonical + `cctpHLDomain` correct + CCTP liveness; cap `cctpActivationUSDC`; timelock. |
| **VARO: `house` lock + bootstrap one-shots** | **VARO** | `house`=KMS; `vgcDeployer`/`vgcRecipient`=0; `vdaoBootstrapped`=false | **VARO-L4.** `setHouse` locks forever after `bootstrapVGCVDAO`; `setVgcDeployer`/`setVgcRecipient` are one-shot; `bootstrapVGCVDAO` is one-shot/latched (deployer pre-funds own VY/V-DAO/asset, no mint/cap touch). Set deployer/recipient correctly before triggering. |
| **VARO deps NOT yet audited** | VDAOFactory / VDAO token / VDAO-DAX / hlFactory / VPO | — | T4/perp paths trust the **ValinityVDAOFactory** (deploys immutable V-DAO tokens), the **V-DAO token** (0.7% fee/burn; VARO fee-exempt), the **VDAO-DAX** `0x37Cd61b3` (sibling AMM), **hlFactory** (HyperEVM/CCTP), and **VPO**. Audit before enabling T4/perp. |
| **VDAO-DAX: `_authorizeUpgrade` = liquidity boundary** | **VDAO-DAX** | admin = KMS `0x8310eA7E`; no timelock | **VDX2-C1 dominant.** The permanently-locked-liquidity guarantee rests ENTIRELY on the impl having no withdraw path — a malicious UUPS upgrade can add a drain and pull ALL V-DAO second-leg reserves. Stronger tension than most (liquidity is *permanent* by design → any added exit is pure theft). Migrate DEFAULT_ADMIN+ADMIN together → TimelockController + real upgrade delay; renounce EOA. |
| **VDAO-DAX: `swapWhitelist` + `assetAllowed`** | **VDAO-DAX** | swappers: none live; assets {USDC,WBTC,WETH,PAXG} | **VDX2-M1/M2.** Swaps are value-conserving (no free drain) but only whitelisted parties move reserves — freeze the swap-whitelist to audited contracts (the MEV bot) before lock. `assetAllowed` poison-asset can brick its OWN pool (pools are isolated — no shared-basket contamination, unlike main DAX). Curate both. `mainDax` is init-only immutable (✅ = real DAX `0xD256C672`). |
| **VDAO-Factory: `_authorizeUpgrade` + `setVeo`/`setMainDax`** | **VDAOFactory** | admin = KMS `0x8310eA7E`; varo/veo/mainDax wired to real | **VDF-C1/M1.** UUPS factory controls how ALL **future** V-DAOs are minted — a malicious upgrade bakes attacker-exemptions / mints to attacker / re-points the post-mint forward. `setVeo`/`setMainDax` bake the permanent fee-exempt wiring into every future token. **Deployed V-DAO tokens are immutable** (unaffected). Migrate DEFAULT_ADMIN → TimelockController + delay; renounce EOA; confirm veo=`0x48C88B80`/mainDax=`0xD256C672`. (The deployed `ValinityVDAO` token has NO admin/upgrade — nothing to lock there.) |
| **⚠ VARO workspace edited (pending redeploy?)** | **VARO** | live impl `0xeC4B6401` = audited (UNCHANGED) | The workspace `ValinityAllianceRegistrationOfficer.sol` now shows git `M` (edited since the 2026-06-05 audit). The LIVE VARO impl is still the audited bytecode, so the VARO audit holds for the deployed contract. **If VARO is redeployed, re-audit before relying on it.** |
| _(more added as each contract is audited)_ | | | |

## Diagrams

| # | Contract | Diagram | Circuit verdict |
|--:|----------|---------|-----------------|
| 1 | ValinityToken (VY) | [ValinityToken-fund-flow.md](ValinityToken-fund-flow.md) | ✅ Closed — one bounded fee valve (→ VBBO, ≤10%, never principal) |
| 2 | ValinityYieldTreasury (VYT) | [ValinityYieldTreasury-fund-flow.md](ValinityYieldTreasury-fund-flow.md) | ✅ Closed (non-admin) — only officer `pullTokens` exits; admin powers excluded by design |
| 3 | ValinityAcquisitionOfficer (VAO **V2**) | [ValinityAcquisitionOfficer-fund-flow.md](ValinityAcquisitionOfficer-fund-flow.md) | ✅ CLOSED (non-admin) **by physics** — permissionless `executeAcquireByLTV/MTP`; VY→DAX→VRT with two balance invariants (zero retention); rescue blocks VY. Dep CC-1: DAX swap must be permissioned. |
| 4 | ValinityMEVBotV2 (VMB) — **RE-AUDIT (new impl)** | [ValinityMEVBotV2-fund-flow.md](ValinityMEVBotV2-fund-flow.md) | ✅ CLOSED (permissionless flow) — **principal can't be exfiltrated even by a malicious INTERMEDIATE router** (VY→…→VY loop + SwapOutputZero + TokenInNotDrained + finalVY≥borrowVY + VYBalanceNotZero ⇒ any principal diversion → atomic pullTokens rollback). **COMPLETE REDESIGN**: removed arbitrary `router.call(callData)`+selector allowlist (old CC-1 — GONE); contract now builds each swap (recipient=this, minOut=0). proxy `0x6f2F4580` upgraded `0x16b66a22`→`0x98d7ee49` (source==live metadata-proven). Residual (admin/upgrade-gated): **C-1** `_authorizeUpgrade` (inherits VYT.OFFICER_ROLE → **cushion-bounded ~6.65M VY/call**); **H-1** malicious final-leg router skims profit-above-floor (100% if minProfitVY=0); **H-2** setMinProfitVY=0; **M-1** setVgo unaudited-VGO trust (`vgo=0` live); **M-2** dual-role handoff. Whitelist=UniV2 Router02+UniV3 SwapRouter02; 70 VY floor + 7d cooldown. Workflow `wc33q498d` (26 surv/29 ref). |
| 5 | ValinityReserveTreasury (VRT) | [ValinityReserveTreasury-fund-flow.md](ValinityReserveTreasury-fund-flow.md) | ✅ CLOSED relative to roles — reserve vault, NO permissionless entry; exits gated OFFICER(VLO)/BUYBACK(VBBO)/VRYO/VLM. Source==live proven by metadata-hash (full 816-line). |
| 4 | ValinityCapOfficer (VCO) | [ValinityCapOfficer-fund-flow.md](ValinityCapOfficer-fund-flow.md) | ✅ CLOSED ledger (MAX-RIGOR) — **the backing/accounting ledger; holds NO tokens.** Every cap/registry/utilization mutator OFFICER/ADMIN-gated; **0 permissionless-exploitable**; arithmetic sound. **VY CANNOT be under-backed** via any permissionless/view/oracle path. **2 Medium: V3-LP undercount** (TVL/LTV-F read only raw `balanceOf(VRT)`, ignore VRT V3-LP NFTs ~100×; conservative reshuffle, no under-backing; not fixed in newer ver) + **unaudited VGO holds OFFICER_ROLE**. Golden-rule conservation enforced per-OFFICER (VCO is faithful but BLIND). Source==live `e268bbd` (workspace ahead). Workflow `wdkc3ozml` (10 surv/77 ref). |
| 5 | ValinityAcquisitionOfficer (VAO) | _pending_ | — |
| 6 | ValinityLoanOfficer (VLO) | [ValinityLoanOfficer-fund-flow.md](ValinityLoanOfficer-fund-flow.md) | ✅ CLOSED (non-admin) — rent-to-own lending; outflows→{borrower, VRT, VBBO, VYT, fee}, no arbitrary dest. Source==live recompile-proven. Lending math verified sound (workflow `wy5ct28gd`). 1 Med VLO-M1 (migrateLoans cap — accepted, one-time bootstrap, locked after gov). **user OK 2026-06-01**. |
| 7 | ValinityBuybackOfficer (VBBO) | [ValinityBuybackOfficer-fund-flow.md](ValinityBuybackOfficer-fund-flow.md) | ✅ CLOSED (permissionless) — system fee sink; retires VY→VYT, recycles VRT asset→VY via DAX. All sinks hardcoded; `rescueToken` ADMIN-only + VY-blocked; economics value-conservative. 0 Crit/High, **1 Medium (CC-1: `minOut=0` DAX dep — HARD PREREQ audit DAX before handoff)**, 1 Low (VGC donation, dormant), rest Info. Source==live byte-identical (`b77d8d7`). Workflow `wrc1qyt6q` (9 surv/1 ref). **5 DAX + 2 VCO obligations.** |
| 8 | ~~ValinityLiquidityManager (VLM)~~ — 🗑️ **ELIMINATED 2026-06-07** | — | 🗑️ **REMOVED FROM THE SYSTEM.** The Uniswap-V3-LP reserve-yield strategy was replaced by the VRYO→DAX reserve-injection model. VLM `0x920AbB09` holds **ZERO roles anywhere** (VRT/VCO/DAX, all verified false on-chain). Prior audit (`wlekavf32`, impl `0xcB147742`) is **historical/N-A** — not a handoff item. |
| 9 | ValinityYieldOfficer (VYO) | [ValinityYieldOfficer-fund-flow.md](ValinityYieldOfficer-fund-flow.md) | ✅ CLOSED (non-admin) by physics — yield engine + VSR backstop; **VY minter on VYT**. VY minted at only 4 `pullTokens` sites; permissionless claims msg.sender-scoped + recipient-hardcoded + clamped to `maxGross`; topUps onlyRouter(VSR). **0 permissionless-exploitable.** Mint **bounded** (9% cap, frozen snapshot, self-throttling), **backed** (every pull booked 1:1 into VCO = **cap golden-rule increment**), **value-conserving**. Residual = admin-trust; **dominant lever `_authorizeUpgrade`** (upgrade inherits OFFICER_ROLE → unbounded mint) > setRouter. Workflow `w0ximbq0f` (5 surv all-Info/Low / 49 ref). **VYT + VCO + VARO + VSR obligations logged.** |
| 10 | ValinityReserveYieldOfficer (VRYO) — **RE-AUDIT (V3 redesign, VLM-free)** | [ValinityReserveYieldOfficer-fund-flow.md](ValinityReserveYieldOfficer-fund-flow.md) | ✅ CLOSED — **no arbitrary-dest ANYWHERE** (all sinks hardcoded {DAX inject, VRT recall/rescue}; `reserveExtract` recipient role-gated→VRT; officer holds nothing between txs). **Cap-conserving CLOSED LEG** (`globalCap = vco.getAssetCap + capVRYO` symmetric — workflow refuted phantom-cap/under-back). Now deploys reserve as one-sided DEPTH into the OWN DAX pools (`reserveInjectAsset`), not Uniswap-V3 LP. Residual: **C1** `_authorizeUpgrade` amplified by 3 roles (drain DAX asset + pull VRT + mutate VCO caps); **M1** BACKING-QUALITY shift (deployed asset arb'd→VY-in-pool; `deployedAsset` overstates live reserve — governance-accept+MONITOR); **M2** partial-recall keeper risk; Lows. **LIVE+FUNDED** (60%, ~226k VY cap deployed). impl `0xc8b848b9`, source==live metadata-proven (HEAD `1aa24be`). Workflow `wqm7zyrs0` (26 agents, 13 surv/6 ref, **0 permissionless**). |
| 11 | ValinityDAX (DAX) — **RE-AUDIT (+ reserve-officer injection)** | [ValinityDAX-fund-flow.md](ValinityDAX-fund-flow.md) | ✅ CLOSED relative to roles/whitelists — **ZERO permissionless entry**; swap/LP/AMM core UNCHANGED & value-conserving (carries `w91m0w9q5`). **NEW: `RESERVE_OFFICER_ROLE` (VRYO-only) + `reserveInjectAsset`/`reserveExtractAsset`** (asset-leg only, VY untouched, no VDAX mint/burn, nonReentrant). Residual: **H1** RESERVE_OFFICER drains pool ASSET (never VY) to arbitrary dest — VRYO-only/admin-equivalent; **H2** DAX upgrade/`revokeRole` strands VRYO assets (shared-KMS cross-bricking); **M3** `reserveInjectAsset` nominal-not-delta → PAXG over-count (VRYO compensates); M1/CC-1 (VEO+SR); Lows. **VLM removed; MEV bot now swap-whitelisted.** impl `0x7c240521`, source==live metadata-proven. Workflow `wqm7zyrs0` (**0 permissionless**). |
| 12 | VDAX | _pending_ | — |
| 13 | ValinityExchangeOfficer (VEO) | [ValinityExchangeOfficer-fund-flow.md](ValinityExchangeOfficer-fund-flow.md) | ✅ CLOSED (non-admin) — public swap router; every exit ∈ {msg.sender, VBO, vdaoCreator, transient self}, no caller-supplied recipient. **0 Crit/High/Med/Low** (all Info). **CC-1 fully discharged: NO atomic sandwich** (one swap/tx, nonReentrant, no multicall; officers don't route through VEO). Source==live metadata-proven (`1220f6c52b99`). Live **DORMANT** (`varo=0`, 0 traders). Workflow `wqoqdl1g7` (5 surv all-Info/32 ref). Handoff: migrate **ADMIN_ROLE** (not just DEFAULT_ADMIN). **VARO + StakingRouter + officer-MEV obligations logged.** |
| 14 | ValinityStakingRouter (VSR) | [ValinityStakingRouter-fund-flow.md](ValinityStakingRouter-fund-flow.md) | ✅ CLOSED (non-admin) — staking vault; every exit ∈ {msg.sender, buybackOfficer}, LP custody never leaves except burned back in. **0 permissionless-exploitable.** LIVE (~564k VY staked, 765k VDAX). **CC-1/DAX-L1 DISCHARGED** by ≥30-day lockup + principal-clamp (surplus→BBO) + admin-frozen index. Value-conserving (exactly-principalVY; YO reserve non-drainable). Residual = **admin-trust: 1 Crit (UUPS=DEFAULT_ADMIN, dominant) + 1 High (adminExtract) + 2 Med + 2 Low** — all handoff. Workflow `w5etym0g7` (12 surv/48 ref). **VYO-solvency (HARD) obligation logged.** |
| 15 | _(ValinityMEVBotV2 — see row 4, re-audited new impl)_ | — | — |
| 16 | ValinityGasOfficer (VGO) — **RE-AUDIT (curve+tip v5, now LIVE)** | [ValinityGasOfficer-fund-flow.md](ValinityGasOfficer-fund-flow.md) | ✅ No custody drain — **keeper-reward engine** (mints **VGC** only; VGO=sole locked minter) to officer-supplied keepers; reward = `min(gasUsed·(baseFee·mult + min(tip,tipCap)), 0.005 ETH)`, mult decays **7×→1.25×** by VGC supply. **DOUBLE-bounded** (per-call cap + VGC epoch ceiling); premium on basefee only (unmanipulable), tip capped 1:1 (no overpay). Holds no funds, **pulls no VY, mutates no VCO cap.** proxy `0x0a6C2117`→impl `0x70F44166` (source==live metadata-proven). **NOW LIVE** (vgc wired, minter==VGO, isPayoutReady, 59 RewardPaid). **VGO-H1 RESOLVED — `VCO.OFFICER_ROLE(VGO)=FALSE`** (revoked ✅). Even a malicious upgrade is bounded to VGC emission (no VY/reserve/cap reach). Residual: **VGO-D** _authorizeUpgrade (sole minter, capped by VGC ceiling — co-locate w/ VGC.admin); **L1** compromised-officer farming/budget-drain (bounded); **L2** whitelisted-swapper mid-price skew; **L3** stale pool-id on DAX reindex (re-run wireVgc). OFFICER={VBBO,VAO `0x7a0E5824`,VMB,VARO,VFO} all audited. Workflow `wu2ofpcrf` (86 agents, **0 permissionless-exploitable**). |
| 17 | ValinityFloorOfficer (VFO) — **RE-AUDIT (permissionless UUPS, migrated)** | [ValinityFloorOfficer-fund-flow.md](ValinityFloorOfficer-fund-flow.md) | ✅ CLOSED relative to roles — **no arbitrary-dest exfil even though `executeFloor` is now PERMISSIONLESS.** Flash-loan floor-defense **migrated** old `0x3d9d78CD` (operator-gated, non-upgradeable) → **new UUPS proxy `0x79A902864d`** → impl `0x33c3DD5c`. Routing HARDCODED (no operator, no router whitelist, no caller calldata). Every sink hardcoded {VYT burn, VFO-self, Balancer repay, **VBBO profit — not caller**}; `caller_` used ONLY in the event; no delegatecall/arbitrary call. **Golden-rule TEXTBOOK & caller-immune** (`decreaseAssetCap(vyAmount)`==VY-to-VYT; `withdraw=vyAmount·reserve/cap` preserves backing; double floor-guard). Permissionless SAFE (profit→VBBO, bad sizing reverts atomically, sandwich harms only caller, flash-spoof blocked, keeper-farming bounded). Residual (ALL admin/handoff): **C1** `_authorizeUpgrade` master drain lever amplified by 3 roles (VRT.BUYBACK→sweep 100% reserves [Crit] + VCO.OFFICER→unbounded increaseAssetCap [High] + VGO.OFFICER); **H1** venue setters redirect circuit; **M1** backing-ratio read excludes VRYO-deployed (conservative MONITOR, ties VRYO-M1); **M2** migrate BOTH roles atomically; Lows (vryo.execute() un-try/catch'd=liveness, PAXG-FoT DoS, sandwich skims surplus, rescueToken no-reserve-block). Roles: VRT.BUYBACK✅+VCO.OFFICER✅+VGO.OFFICER✅+VY-WL✅ (old revoked on all). Source==live metadata-proven (HEAD `1aa24be`). Workflow `wu2ofpcrf` (86 agents, 39 raw→37 surv/2 ref, **0 permissionless-exploitable**). |
| 17b | ValinityGovernanceCommittee (VGC) | [ValinityGovernanceCommittee-fund-flow.md](ValinityGovernanceCommittee-fund-flow.md) | ✅ Emission provably bounded — **the VGO's reward token** (gov + keeper-funding). **NON-PROXY / immutable.** Plain ERC20+Permit, **no fee/no burn/no hook/no admin-mint/no balance-move/no upgrade.** Per-epoch ceiling ≤`epochMintBps`(≤0.25%/wk) of UNMINTED supply (frozen at epoch start); totalSupply asymptotic < MAX_SUPPLY 7M forever; `epochMintBps=0`=halt; ≤immutable `MAX_EPOCH_MINT_BPS=25` (admin can only slow/halt). Sole minter ONE-TIME-locked. **Validates VGO-M2.** Live: totalSupply=1M (seed→KMS), **minter=0x0 UNSET (doubly-dormant)**, admin=`0x8310eA7E`. Residual: L1 one-time setMinter activation (point at audited VGO), L2 admin halt, L3 1M seed at KMS (gov centralization). Workflow `w5mu2e3r7`. |
| 18 | ValinityAllianceRegistrationOfficer (VARO) — **BRAND-NEW** | [ValinityAllianceRegistrationOfficer-fund-flow.md](ValinityAllianceRegistrationOfficer-fund-flow.md) | ✅ CLOSED relative to roles — **no arbitrary-dest exfil in any non-admin path.** Paid-tier gateway + referral-revenue accountant + V-DAO launcher (1823+287 ln; UUPS + DELEGATECALL lib). Every exit ∈ {VBBO hardcoded init-only, msg.sender, `predictBuilder(msg.sender)`, bound V-DAO+pools, AMM pools}; **no `setVbbo`/`setVy`/`setVyt`/`setVco`, no `rescueToken`**. **VY mint golden-rule-correct** (only `_payOut`: `pullTokens(paid)`+`addToHighestLTVFCap(paid)`; pullTokens reverts-on-shortfall/never-partial → exact cap, cushion-throttled, custody-0). **Referral credit checkpoint-idempotent** (settleMine/sweep), bounded by real fees×bps (≤99%). Residual (ALL admin-trust/handoff/ops, **0 permissionless-exploitable**): **C1** `_authorizeUpgrade` inherits ALL officer roles once wired (dominant); **H1** `setHlFactory` redirects every user's CCTP $100 (admin-rotatable, 4 entry pts, no L1 recovery); **H2** `setTierUsdcPrice` no floor/ceiling (free tiers/launches/DoS); M1 un-checkpointed PUSH (`vpo`,`REVENUE_PUSHER_ROLE`) → cushion-bounded unbacked VY to favored referrer; M2 `cctpActivationUSDC` no max → MEV-inflated payment (stays closed); M3 CCTP-fail+`hasBuilder`-before-confirm → $100 stuck no recovery; M4 95% bps mint magnitude (by-design); M5 one-shot vgcDeployer/Recipient irreversible; Lows. Source==live metadata-proven (impl+lib, git-clean `a85f449`). **LIVE: FULLY DORMANT/UN-WIRED** (holds no roles, factory/cctp/hlFactory=0, zero activity). Workflow `wau2dte5v` (30 agents, 17 surv/4 ref). |
| 19 | ValinityVDAODAX (VDAO-DAX) — **BRAND-NEW** | [ValinityVDAODAX-fund-flow.md](ValinityVDAODAX-fund-flow.md) | ✅ CLOSED / liquidity permanently LOCKED relative to roles — V-DAO second-leg AMM (asset/V-DAO + V-DAO/V-DAO pools), sibling of main DAX, constant-product NO-fee, **arbitrary** token pairs. **NO LP token, NO deposit/withdraw/remove/skim** — reserves only grow (addPool/donate/swap-in) or shrink via a value-conserving **k-preserving** swap (`swapExactIn`, onlySwapWhitelisted); `rescueTokens` **reverts on isPoolToken** (admin can't pull pool reserves). FoT-safe balance-delta accounting; pools **isolated** (no shared basket, unlike main DAX); `donate` permissionless but add-only. Residual (ALL admin-trust/handoff/ops, **0 permissionless-exploitable**): **C1** `_authorizeUpgrade` IS the liquidity-safety boundary (malicious upgrade adds a drain → all reserves in one tx; no timelock); M1 swap-whitelist rogue-swapper MEV (no pool drain); M2 assetAllowed poison-asset (pool-isolated, contained); M3 output-leg fixed-decrement = recipient-side-FoT-only assumption (gated by asset curation); Lows. Source==live metadata-proven (impl, git-clean `a85f449`). **LIVE DORMANT** (getNumPools=0, no swappers whitelisted, mainDax=`0xD256C672` real DAX✅, VARO=POOL_CREATOR✅). Workflow `wvrnl84j3` (23 agents, 8 surv/9 ref). |
| 20 | ValinityVDAOFactory + ValinityVDAO — **BRAND-NEW** | [ValinityVDAOFactory-fund-flow.md](ValinityVDAOFactory-fund-flow.md) | ✅ CLOSED / no fund leak — **and DISCHARGES both DEX assumptions.** Factory (UUPS) = sole deployer + source-of-truth (name derivation, FCFS uniqueness, bakes permanent veo+mainDax exemptions; launch/reserveName VARO_ROLE-only; patches FUTURE launches, deployed tokens frozen). Token `ValinityVDAO` = **fully immutable** ERC20+Permit+Burnable (no admin/owner/upgrade/setter); mints exactly 2× activeSupply (active→VARO, locked half→1-yr linear creator vest, CEI/no-over-claim); 0.7% fee BURNED. **3 KEY PROPERTIES HOLD: RECIPIENT-SIDE fee** (sender drops exactly `value`→DEX reserve stays in sync) **→ discharges VDAO-DAX-M3**; **HOOK-FREE** (no callbacks) **→ discharges DEX no-reentrancy**; **main-DAX exempt / VDAO-DAX not**. Residual (ALL admin-trust/handoff/ops, **0 permissionless-exploitable**): **C1** factory `_authorizeUpgrade` (controls FUTURE minting; deployed tokens immutable; no timelock); **H1** setVeo/setMainDax weaponize future-token exemptions; M1 creator-vest 50%-of-max/1yr (tokenomics, creator's own token, not protocol VY); Lows. Source==live metadata-proven (factory impl; dirty-workspace==as-deployed). **LIVE: wired, no V-DAO launched yet.** Workflow `wg9tl6qlr` (12 agents, 3 surv/3 ref; **3 KEY PROPERTIES CONFIRMED**). |
| 21 | ValinityPortal | _pending_ | — |

> Convention: every diagram annotates each value edge as ✅ fixed/internal/rightful-holder or ⚠️ settable/arbitrary destination. A contract is "closed circuit" only if no edge can send **principal** to an arbitrary address. Settable destinations that survive to handoff go in the watchlist above.
